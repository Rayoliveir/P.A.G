/* Import Google Fonts */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');

/* Reset and Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    line-height: 1.6;
    color: #1f2937;
    background-color: #ffffff;
}

/* Header Styles (Copied from home.css) */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    position: sticky;
    top: 0;
    z-index: 1000;
}

.logo-PAG {
    display: flex;
    align-items: center;
}

.logo-PAG img {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    border-radius: 8px;
    margin-right: 0.75rem;
}

.titulo-PAG h3 {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.link-paginas {
    display: flex;
    gap: 2rem;
}

.link-paginas a {
    text-decoration: none;
    color: #4b5563;
    font-weight: 500;
    transition: color 0.3s ease;
    position: relative;
}

.link-paginas a:hover {
    color: #8b5cf6;
}

.link-paginas a::after {
    content: '';
    position: absolute;
    width: 0;
    height: 2px;
    bottom: -4px;
    left: 0;
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    transition: width 0.3s ease;
}

.link-paginas a:hover::after {
    width: 100%;
}

.log-cad {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.log-cad a {
    text-decoration: none;
    color: #4b5563;
    font-weight: 500;
    transition: color 0.3s ease;
}

.log-cad a:hover {
    color: #8b5cf6;
}

.botao-cadastrar {
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px -1px rgba(139, 92, 246, 0.3);
}

.botao-cadastrar:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 15px -3px rgba(139, 92, 246, 0.4);
}

.botao-cadastrar a {
    color: white !important;
    text-decoration: none;
}

/* Footer Styles (Copied from home.css) */
footer {
    background: #1f2937;
    color: white;
    padding: 3rem 2rem 1rem;
}

footer > div:first-child {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto 2rem;
}

.footer-sobre, .footer-plataforma, .footer-suporte {
    display: flex;
    flex-direction: column;
}

.footer-sobre img, .footer-plataforma img, .footer-suporte img {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    border-radius: 8px;
    margin-bottom: 1rem;
}

.footer-sobre h3, .footer-plataforma h3, .footer-suporte h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
}

.footer-sobre p {
    color: #9ca3af;
    line-height: 1.6;
}

.footer-plataforma a, .footer-suporte a {
    color: #9ca3af;
    text-decoration: none;
    margin-bottom: 0.5rem;
    transition: color 0.3s ease;
}

.footer-plataforma a:hover, .footer-suporte a:hover {
    color: white;
}

footer hr {
    border: none;
    height: 1px;
    background: #374151;
    margin: 2rem 0 1rem;
}

/* Main Content */
main {
    flex: 1;
    padding: 3rem 2rem;
    background-color: #f8fafc;
}

.desafios-header {
    text-align: center;
    margin-bottom: 3rem;
}

.desafios-header h1 {
    font-size: 3rem;
    font-weight: 800;
    color: #1f2937;
    margin-bottom: 1rem;
}

.desafios-header p {
    font-size: 1.2rem;
    color: #6b7280;
    max-width: 700px;
    margin: 0 auto;
}

.filtro-desafios {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 3rem;
    flex-wrap: wrap;
}

.filtro-desafios button {
    background: white;
    border: 1px solid #d1d5db;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    color: #4b5563;
}

.filtro-desafios button:hover {
    border-color: #8b5cf6;
    color: #8b5cf6;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
}

.filtro-desafios button.active {
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    color: white;
    border-color: #8b5cf6;
    box-shadow: 0 4px 6px -1px rgba(139, 92, 246, 0.3);
}

.lista-desafios {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
}

.card-desafio {
    background: white;
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    border: 1px solid #e5e7eb;
    display: flex;
    flex-direction: column;
}

.card-desafio:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px -5px rgba(0, 0, 0, 0.1);
}

.card-desafio img {
    width: 60px;
    height: 60px;
    border-radius: 12px;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #3b82f6, #06b6d4); /* Placeholder background */
}

.card-desafio h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: #1f2937;
}

.card-desafio p {
    color: #6b7280;
    line-height: 1.6;
    margin-bottom: 1rem;
    flex-grow: 1; /* Permite que a descrição ocupe o espaço */
}

.desafio-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1rem;
}

.desafio-info .dificuldade {
    background: #e0e7ff;
    color: #4f46e5;
    padding: 0.4rem 0.8rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
}

.desafio-info .dificuldade.iniciante {
    background: #d1fae5;
    color: #059669;
}

.desafio-info .dificuldade.intermediario {
    background: #e0e7ff;
    color: #4f46e5;
}

.desafio-info .dificuldade.avancado {
    background: #fee2e2;
    color: #ef4444;
}

.card-desafio a {
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    display: inline-block;
    margin-top: 1.5rem;
    text-align: center;
}

.card-desafio a:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(139, 92, 246, 0.4);
}


/* Footer */
footer {
    background: #1f2937;
    color: white;
    padding: 3rem 2rem 1rem;
}

footer > div:first-child {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto 2rem;
}

.footer-sobre, .footer-plataforma, .footer-suporte {
    display: flex;
    flex-direction: column;
}

.footer-sobre img, .footer-plataforma img, .footer-suporte img {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
    border-radius: 8px;
    margin-bottom: 1rem;
}

.footer-sobre h3, .footer-plataforma h3, .footer-suporte h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
}

.footer-sobre p {
    color: #9ca3af;
    line-height: 1.6;
}

.footer-plataforma a, .footer-suporte a {
    color: #9ca3af;
    text-decoration: none;
    margin-bottom: 0.5rem;
    transition: color 0.3s ease;
}

.footer-plataforma a:hover, .footer-suporte a:hover {
    color: white;
}

footer hr {
    border: none;
    height: 1px;
    background: #374151;
    margin: 2rem 0 1rem;
}
/* Responsive Design (Copied from home.css) */
@media (max-width: 768px) {
    header {
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
    }
    .link-paginas {
        gap: 1rem;
    }

    .desafios-header h1 {
        font-size: 2.5rem;
    }

    .filtro-desafios {
        flex-direction: column;
        align-items: center;
    }

    .filtro-desafios button {
        width: 80%;
        max-width: 250px;
    }

    .lista-desafios {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 480px) {
    .desafios-header h1 {
        font-size: 2rem;
    }

    main {
        padding: 2rem 1rem;
    }
}

/* Acessibilidade: foco */
button:focus, a:focus {
    outline: 2px solid #8b5cf6;
    outline-offset: 2px;
}

